recta = [];
x = linspace(-1, 1, 500);
y = 2 + zeros(1, 500);

ya = linspace(2, 0, 500);
xa = -1+zeros(1, 500);

xa = linspace(-1, 1, 500);
ya = 2 + zeros(1, 500);
ya = linspace(2, 0, 500);
xa = -1+zeros(1, 500);

x = [x xa];
y = [y ya];
plot(x, y)